pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script {
                   echo  env.BRANCH_NAME
                    }
                }
            }
        }
    }
}
