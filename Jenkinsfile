pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script {
                    for (int i = 0; i < 10; ++i) {
                        echo "NAVEEN = ${i}"
                    }
                }
            }
        }
    }
}
