pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script {
                   bat 'echo %PATH%'
                    }
                }
            }
        }
    }
