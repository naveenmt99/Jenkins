pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script {
                    echo 'NAVEEN'
                    bat 'dir'
                }
            }
        }
    }
}
